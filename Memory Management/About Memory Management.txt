C++ allows us to allocate the memory of a variable or an array in run time. This is known as dynamic memory allocation.

In other programming languages such as Java and Python, the compiler automatically manages the memories allocated to variables. But this is not the case in C++.

In C++, we need to deallocate the dynamically allocated memory manually after we have no use for the variable.

We can allocate and then deallocate memory dynamically using the new and delete operators respectively.

\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\

سی پلاس پلاس  به ما این امکان را می دهد که حافظه یک متغیر یا یک آرایه را در زمان اجرا اختصاص بدهیم. این به عنوان تخصیص حافظه پویا شناخته می شود.
در دیگر زبان های برنامه نویسی مانند جاوا و پایتون، کامپایلر به طور خودکار خاطرات اختصاص داده شده به متغیرها را مدیریت می کند. اما در سی پلاس پلاس چنین نیست
در سی پلاس پلاس باید پس از اینکه هیچ استفاده ای برای متغیر نداشتیم، حافظه تخصیص یافته پویا را به صورت دستی جا به جا کنیم.
ما می توانیم حافظه را به ترتیب با استفاده از اپراتورهای جدید و حذف به صورت پویا اختصاص داده و سپس معامله کنیم.






------------
Amoo-Srwsh |
------------
